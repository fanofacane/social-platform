<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.in_sky1.mapper.OrderMapper">
    <!--根据ID更新员工基本信息-->
    <!--set标签: 会自动生成set关键字; 会自动的删除掉更新字段后多余,-->

    <select id="getOrderByUserId" resultType="com.example.in_sky1.dto.OrderVO">
        SELECT o.id,o.user_id,o.product_id,p.title,p.picture,o.state,o.numbers,p.price,o.create_time
        FROM orders o left join products p on o.product_id=p.id
        WHERE o.user_id= #{userId}
    </select>

</mapper>